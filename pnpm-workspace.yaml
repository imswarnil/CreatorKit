packages:
  - 'packages/*'
  - 'templates/*'
  - 'tools/*'
# Both are wrangler's: esbuild and workerd ship prebuilt binaries their install
# scripts unpack. Without this, `pnpm run` refuses with ERR_PNPM_IGNORED_BUILDS.
allowBuilds:
  esbuild: true
  workerd: true
