/**
 * @creatorkit/icons — 55 icons in six sets: creator, media, resume, social,
 * travel and ui.
 *
 * Drawn on a 24px grid with `stroke="currentColor"` and no intrinsic size, so
 * they take their colour from the text around them and their size from the
 * kit's icon scale.
 *
 * Two ways to use one:
 *
 *   // sprite — one request, any number of icons
 *   <svg class="icon icon-md"><use href="/sprite.svg#ck-rec" /></svg>
 *
 *   // React
 *   <Icon name="rec" size="md" />
 */
export { iconNames, iconSets } from './names.js';
export type { IconName } from './names.js';
